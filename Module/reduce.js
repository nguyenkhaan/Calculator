const init = {

} 
function reducer(state = init, action, args) {
    switch (action) 
    {
        default: return state; 
    }
} 
export default reducer 
//Neu khong co default thi phai export 1 cai Object 